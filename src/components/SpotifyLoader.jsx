const SpotifyLoader = () => {
  return (
   <div className="flex items-center justify-center gap-2 h-full">
      <span className="w-3 h-3 bg-white/50 rounded-full animate-bounce [animation-delay:-0.3s]"></span>
      <span className="w-3 h-3 bg-white/50 rounded-full animate-bounce [animation-delay:-0.15s]"></span>
      <span className="w-3 h-3 bg-white/50 rounded-full animate-bounce"></span>
    </div>
  );
};

export default SpotifyLoader;